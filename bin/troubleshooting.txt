This folder is a folder for all the files the script installs, if something was not installed right, execute the application again. If you encounter an errors, use the log avalible in the script root. Get any errors? Make sure it isn't from the GPU updater script. The log will explain these errors in detail. 
